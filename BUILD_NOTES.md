# 入藏 · 西藏12日 App — 交接紀錄

最後更新：2026-08-20｜sw.js 快取版本：tibet12-v11

這份文件跟 `index.html` 是一組的，用來讓下一個對話（或未來的你）不用重新讀完整份程式碼就知道現況。`index.html` 最上方也埋了同一份精簡版註解，兩邊保持一致。

---

## 現況一句話

單一 HTML 檔、純 JS 渲染、無框架、無資料庫，資料寫死在 script 內的常數裡。改內容＝改那些常數，不用碰版面。已部署 GitHub Pages，離線可用，7 個檔案需同層：`index.html`、`manifest.json`、`sw.js`、`robots.txt`、`icon-180.png`、`icon-192.png`、`icon-512.png`。

## 已完成功能

- **今日**：依系統日期自動切換出發前／旅途中／已結束，含倒數、當日待辦（可勾選，本機存）、明日預告、海拔曲線（含「你在這」標記）
- **9/2 阿玫生日卡**：只在當天出現，夜空＋經幡動畫，每次進今日頁重播一次，文案已定稿
- **行程**：D1–D12 逐日卡片、可收合的西藏區域地圖（喜馬拉雅稜線／雅魯藏布江／三湖，經緯度手算投影，非行政邊界）、成都自由時間 3 格（已填）、高海拔警示兩級（altLevel，lv2=過夜警戒／lv1=白天暴露）＋圖例、朝聖圖風格美術（層疊山稜／水紋暈圈／邊框／比例尺／指北針）、當日路線依序脈動＋已走過路段淡化
- **交通**：責任軌（灰＝自理、金＝團方接管）、狀態膠囊在標題列
- **住宿**：地址一鍵複製、電話一鍵撥打、當晚自動反白
- **備忘**：緊急處置（HACE/HAPE 警訊、步態測試）、血氧判讀表、丹木斯方案、現金換算、寺院禮儀通則、可編輯聯絡欄位（導遊／司機／車牌，本機存）、字級切換（標準／大）、尚待確認清單
- 待辦項可跨頁連結（點了直接跳備忘並展開對應區塊）
- 離線：service worker 快取所有靜態資源；noindex＋robots.txt 防搜尋引擎收錄

## v11 這次改了什麼

1. **地圖美術大改版**，方向是「藏地朝聖圖／舊版銅刻地圖」。山改三層退遠稜線（`RIDGE_B/M/F`）；湖與河加水紋暈圈（同路徑疊三層粗細遞減的描邊）；非當日元素全改中性暖褐灰，**紅色只留給當日路線與當日地名**；紙色另開 `--map-paper` #F5EEE0 並加 feTurbulence 顆粒；雙線邊框＋四角方塊＋比例尺＋指北針（`MDECOR`）；字體改襯線拉字距；珠峰標記改金色三角；貢嘎機場改菱形。
2. **當日路線圓環依行進順序脈動**（`.mping`，delay = idx×0.22s），順帶帶出行進方向。有 `prefers-reduced-motion` 保護。同一天重複出現的節點（如 9/5 拉薩→納木措→拉薩）只產生一個環。
3. **已走過的路段淡化**（`.mseg.past`）：用 CUR 之前每天的 DROUTE 相鄰節點對建 Set 比對，今日路線本身不算 past。
4. **緊急處置改預設收合**。注意：原本預設展開是刻意的（「要在需要之前讀」），這是使用者要求改的，不是疏漏。
5. **尾款**：8/21 付款，已從 TODOS 移到 PREP 可勾選項。
6. **9/7 送機時間**：Kira 回覆到當地與導遊討論，出發前不會有答案。TODOS 保留但改標「到當地問」，交通頁膠囊改「當地確認」，D11 加了待辦提醒當晚要問到。尚待確認區塊補了 `id="dt-todo"`。

## 已知未完成

1. **中文襯線字靠系統 fallback**（Georgia 對中文無效，實際落到蘋方或宋體），iOS 與 Android 長相會不同。要統一得引一支中文襯線 webfont，但會增加離線快取體積，目前選擇不做。
2. 地圖縱向放大約 2 倍，地理比例失真，caption 有註明。

## 待 Kira 確認、一旦有答案要同步進 App

1. **G06 櫃檯開櫃／方勤到場時間** → 會改到 D1 的今日待辦文字。這是唯一還真的懸著、且出發前有機會拿到答案的一項。
2. ~~尾款~~ 已解決：8/21 付款，改列 PREP 待辦。
3. ~~9/7 送機時間~~ 已有答案：到當地與導遊討論，出發前不會有結果，已改成 D11 晚上的待辦。

## 操作備忘

- **每次改 index.html，記得把 sw.js 的 CACHE 版本號往上跳一碼**，否則手機吃舊快取看不到更新。sw.js 內有 skipWaiting()＋clients.claim()，所以新版會主動接管，不必等所有分頁關閉；但 iOS 對 PWA 不總是照規範走，仍建議完全關閉重開兩次
- 換圖示後，手機主畫面的舊圖示要**刪掉重加**，不會自動更新
- 字型走 Google Fonts CDN，靠 service worker 的 fetch handler 快取，但**第一次載入時 SW 通常還沒接管頁面**，字型請求進不了離線快取。所以出發前建議把 App **完整開啟 2–3 次**（不是只加到主畫面那一次），再用飛航模式驗證

## 一個会影响整个交接节奏的现实限制

**中國大陸境內連不上 claude.ai。** 8/27 進藏後使用者實質上與 Claude 斷聯，App 是唯一資料來源。後續更新只能在 **8/25 出發前**，或 8/25–26 成都期間（網路不保證）完成。這代表所有想加進 App 的內容，都該在這個時間窗口內講完、做完，不要指望在西藏境內還能繼續討論。
